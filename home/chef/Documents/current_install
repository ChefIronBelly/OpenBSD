# OpenBSD 7.9-current

# start w latest OpenBSD-7.9 current snapshot

default install GPT and using UFS default sizes

# pkg_add github-cli git firefox geany mc mpv yt-dlp mpg123 liberation-fonts
scrot fastfetch

# echo permit persist :wheel > /etc/doas.conf
logout

****
as user

$ mkdir ~/Source & cd Source
$ git clone https://github.com/ChefIronBelly/OpenBSD.git
cp user files and root files from /boot /etc /home to system

$ cd Source /dwm /dmenu /st and doas make clean install
$ doas mkdir /mnt/usb

# 260707
